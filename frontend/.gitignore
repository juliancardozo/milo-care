node_modules/
dist/
.env
*.log
coverage/
.DS_Store

# Specs OpenAPI: se versionan para que estén en el build de Amplify (el monorepo
# no expone backend/ en el appRoot del frontend). El prebuild/predev los regenera
# desde backend/openapi.yaml — al editar el spec, re-buildeá y commiteá el cambio.
